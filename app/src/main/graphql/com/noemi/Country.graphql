query Country( $code: ID!) {
  country(code: $code) {
     code
     name
     capital
     currency
     emoji
     continent {
        name
     }
     phone
     languages {
        name
     }
  }
}